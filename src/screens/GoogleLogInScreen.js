import React from "react";
import { Button } from "@react-native-material/core";

const GoogleLogInScreen = () => (
  <Button title="check new" onPress={() => alert("GoogleLogin")}/>
);

export default GoogleLogInScreen;