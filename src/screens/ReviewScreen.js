import React from "react";
import { Button } from "@react-native-material/core";

const ReviewScreen = () => (
  <Button title="Click Me" onPress={() => alert("ReviewScreen")}/>
);

export default ReviewScreen;